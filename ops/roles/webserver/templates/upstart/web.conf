chdir /var/www/{{ site_name }}/{{ site_name }}/
umask 002

setuid www-data

start on runlevel [2345]
stop on shutdown

reload signal SIGHUP

respawn
respawn limit 10 10

script
    /var/www/envs/{{ site_name }}/bin/gunicorn wsgi:application -c workers/{{ env }}.py
end script
